import { createSlice } from '@reduxjs/toolkit'

const initial = []
export const playerInfo = createSlice({
    name:'theName',
    initialState: initial,
    reducers:{
        
    }
})

export const {  } = theName.actions
export default theName.reducer